import React from 'react';

// This component is deprecated. The application now uses a credit-based system.
// API Keys are managed via environment variables on the server/proxy side.

const SettingsModal: React.FC<any> = ({ onClose }) => {
    return null;
};

export default SettingsModal;
